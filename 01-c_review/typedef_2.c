#include <stdio.h>

// Shortcut for typedef in structs
typedef float Real;
typedef struct {
    Real x;
    Real y;
} Point;
typedef struct {
    Point p;
    Real r;
} Circle;

int main() {
    return 0;
}
